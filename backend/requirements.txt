# InvestRing Backend Dependencies
# Generated from backend/deps/ versions (2026-07-12)
# Runtime dependencies

fastapi==0.141.1
uvicorn[standard]==0.52.4
SQLAlchemy==2.0.52
pydantic==2.13.5
pydantic-settings==2.15.0  # issue #252: GHSA-4xgf-cpjx-pc3j 修复版
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.32  # issue #252: PYSEC-2026-3037/3036/3040 修复版（3040 需 0.0.31）
pymysql==1.2.0  # issue #252: PYSEC-2026-502 修复版

# Data
pandas==3.0.5
numpy==2.5.3
python-dateutil==2.9.0.post0

# HTTP & Network
httpx==0.28.1
requests==2.34.2
urllib3==2.7.0  # issue #252: PYSEC-2026-142/141 修复版
certifi==2026.7.22

# Scheduling
apscheduler==3.11.3

# Database migration
alembic==1.19.2

# External data
tushare==1.4.29
# 钉版原因：1.18.90+ 依赖 jsonpath（仅发布 sdist），构建侧需 Dockerfile
# --no-binary jsonpath 豁免（已加）；钉版防上游后续版本再引入新 sdist 依赖
# 破环镜像构建（issue #170 冒烟门禁发现，#196 引入 akshare 后构建即断）
akshare==1.18.94
lxml==6.1.3
simplejson==4.1.2

# uvicorn dependency
click==8.5.0

# Transitive dependencies（显式钉住 fastapi 的传递依赖，issue #314）
# 钉版原因：fastapi 仅声明 starlette>=0.46.0（无上界），故其版本由每次构建时 pip 现场解析决定、
# 不落任何文件；且 pip 不升级「已满足下界」的已装包，导致本地 0.50.0 与 CI 解析的 1.6.0 长期分裂。
# 本行固定为 CI 实际解析并经 CI 验证的版本，消除浮动，使镜像每个包版本均可从仓库读出并 review。
# 升级 fastapi 时必须同步复核本行（取该 fastapi 组合经 CI 验证的 starlette 版本）。
# 注意：本行只闭合 starlette 一处。干净解析实测共 79 个包、本文件仅声明 30 个，
# 其余 50 个传递依赖（anyio、typing_extensions、pydantic_core、cryptography、
# h11/httptools/websockets 等）仍由构建时现场解析、仓库无记录，清单见 issue #314。
starlette==1.6.0

# Auth
python-dotenv==1.2.3

# Dev / Testing（issue #172 全锁：以干净环境实际解析版本为准，
# 本地 + CI 全量测试验证通过；升级需配套重验）
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-mock==3.15.1
factory-boy==3.3.3
